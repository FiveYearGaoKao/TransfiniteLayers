# 架构

> 面向开发者。本文描述代码目录与依赖方向。以【代码】为准,如有出入请修正本文档。

## 一、技术栈

- Vue 3.5 + Vite 7 + TypeScript 5.9,`vue-tsc` 负责类型检查(不要用 `tsc`,`.vue` 会报错)。
- `break_eternity.js`:大数 Decimal,支持指数塔。
- `lz-string`:存档压缩(base64)。
- `@/` 别名 → `src/`。

## 二、目录结构(src/)

```
tools/      纯函数、零依赖(ordinal坐标/softCap软上限/format格式化/utils比较)
data/       类型与常量(player/types/constants/story/changelog)
save/       存档(序列化/校验码/迁移/rng种子随机)
access/     只读访问player的便捷函数(含隐藏成就登记)
compute/    只读计算,不写状态(effects加成管道/dimensions/energy/prestige/upgrades/buyables/buying/knowledge/softCap)
logic/      写状态(update重置purchase/automations/achievements/challenges/knowledge/commands)
meta/       元重置层注册表(infinity等)
core.ts     主循环/暂停/离线
settings.ts 设置(独立localStorage)
log.ts      日志  news.ts 滚动新闻  temp.ts 运行时缓存
dialog.ts   全局对话框系统(confirm/slots/quiz)
uiActions.ts 共享UI动作(重置/加速循环)  commandRunner.ts 指令执行  hotkeys.ts 快捷键
navigation.ts 主标签/层级选择/子标签循环
components/  UI(通用bars + features各页面)
```

## 三、依赖方向(铁律)

```
tools → data → save/access → compute → logic → meta → ui
```

- **单向**:禁止向上引用。
- **compute 只读**:计算函数只读状态并返回结果,绝不写状态。
- **重置先算后写**:`doReset` 先算收益再统一写。
- **例外/注意**:
  - `access` 里极少数写便捷函数(如 `addAmount`、`unlockSecretFlag`)属于例外;
  - UI 编排模块(如 `saveActions`/`commandRunner`/`uiActions`)在根层,可同时引用 dialog(ui) 与 logic;
  - 纯函数(不依赖存档/效果)放 `tools/`;读效果槽/玩家状态的游戏侧包装放 `compute/`。

## 四、核心机制速览

- **加成管道**:见 [effect机制.md](./effect机制.md),所有数值加成经 `registerEffect`/`calculate`。
- **注册表模式**:升级/可购买/自动化/成就/挑战/知识升级/元层/指令都是"定义数组+注册+访问函数"。
- **层级坐标**:任意长数组 `[a1,...,an]` 表示 `ω^(n-1)·a1+…+an`;纯函数在 `tools/ordinal.ts`。
- **层级0特殊**:无重置按钮、维度1产点数(其余层产能量)、energy恒0、永不清除。
- **一帧顺序(全局三段式)**:所有层生产(逆序,高层先) → 元层 onTick → 所有层自动化 → 挑战C5等每帧惩罚 → 成就检查。

## 五、新系统接入清单

新增玩法系统遵循:类型定义(`data/types.ts`)→ 注册表定义 → 效果经 effects 管道 → UI 组件挂到主标签 → 存档字段(含 load 兜底)。
