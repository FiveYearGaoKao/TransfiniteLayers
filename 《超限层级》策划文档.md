# TransfiniteLayers

《超限层级》(Transfinite Layers)是一个含有多层重置层的增量游戏，使用vue.js和vite制作。
该项目使用了2个第三方库：break_eternity.js用来储存大数字，lz-string用来编码存档
目前该项目只实现了初步功能

## 游戏机制

### 层级(Layers)

层级(Layers)是本游戏的核心玩法。本游戏一共有ω^ω个常规层级，每个层级的编号可以用任意长数组表示。
单个层级的玩法类似于反物质维度(Antimatter Dimensions)：第一维度生产点数或能量，第二维度生产第一维度，以此类推。
高层声望重置会清除低层的所有内容，然后根据低层的点数或已解锁的层级数获得高层的点数。

### 无限(Infinity)

无限(Infinity)是第一个元重置层，在达到**1.79e308点数**时解锁。进行无限重置时将重置**所有**后继层级。

无限层的内容比较简单，有三大主要机制：

- 无限升级(Infinity Upgrades): 5x5的简单升级表；
- 元声望(Meta Prestige): 类似反物质维度的维度提升，为每个层级解锁新的维度，或者为维度提供加成；
- 锻造(Forging): 一个分层的制造系统，可以消耗低级产品或基础原料合成高级产品。玩家需要分配不同物品的数量，实现增幅的最大化。

### 奇点(Singularity)

奇点(Singularity)是第二个元重置层，在达到**层级(1,0,0)**时解锁。
该重置层的机制和协同放置(Synergism)的奇点层类似，每进行一次奇点重置，游戏变得更难，但可以解锁更多的特权。

奇点层有两大主要机制：

- 奇点树(Tree of Singularity)，类似反物质维度的时间研究。
- 深渊(The Abyss)，一个类魔塔的小游戏，角色属性和外部独立，但有增幅外部数值的机制。

### 树(Tree)

树(Tree)是第三个元重置层，在达到**层级(1,0,0,0)**时解锁。

树层的主要玩法类似于声望树(The Prestige Tree)，但可以自定义各重置层的连接方式。

### 序数(Ordinal)

序数(Ordinal)是最大的元重置层，在游戏早期(解锁无限之后的某个时间点)解锁，贯穿几乎整个游戏。
序数层的重置被称为**因子转换(Factor Shift)**，因子转换将重置前面的**几乎所有**内容，使序数进制(base)减1.
和序数增量(Ordinal Markup)类似，序数进制初始为10，最低为3，意味着你只能进行7次因子转换。
序数进制能降低层级之间的进位。例如因子转换后只要到达层级9就能解锁层级(1,0)
同时，序数进制能降低**所有层级**中大部分升级的价格。例如，因子转换之后价格为10^n的升级将变为9^n

### 知识

知识(Knowledge)是一种游戏货币，主要通过解锁成就、输入指令、游玩小游戏获得。知识可以购买游戏加成、Qol升级、离线时间，它们不会被重置。

### 挑战

每个重置层都有对应的挑战。所有挑战的本质都是一样的：在一定的条件下进行重置，达成目标后可以获得奖励。

### 自动化

自动化是大部分增量游戏都有的一种功能，其目的是减少玩家的重复操作。
本游戏内置3种自动化模式：

- 内部自动化：如自动购买、自动重置，可以通过1~3个参数进行调整；
- 连点器：可以模拟玩家的一系列点击操作，但只能循环，不支持运行时修改参数；
- 自动机：类似反物质维度的自动机，需要玩家编写“伪代码”，但自由度最高。

## 其它特性(features)

### 反作弊

本游戏没有任何需要强制联网的机制，可以下载至本地游玩。但本游戏为作弊设置了一些门槛：

- 防穿越机制：玩家可以通过修改系统时间来获取离线进度，但只能向后调整。如果你将时间调整至上次保存之前，就无法导入存档；
- 伪随机数：随机数由存档种子决定，不能通过存档/读档修改，也不能通过重写Math.random来控制；
- 存档校验码：玩家可以lz-string的decompressFromBase64函数直接解密存档，但存档额外包含了一个校验码信息：如果修改其他内容而不修改校验码，就无法导入存档。

### 滚动新闻

和大部分增量游戏一样，《超限层级》内置了大量的滚动新闻，包含各个小圈子内的梗(meme)，在挂机的时间为玩家提供一些快乐。

### 指令

可以在“导入存档”框内输入指令。计划实现指令功能有：签到、答题、修改设置、获取彩蛋
